name: flutter_amiibo_responsive
description: Create app connect with amiibo API and setresponsive

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.3
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^7.1.0
  google_fonts: ^2.1.0
  http: ^0.13.3
  intl: ^0.17.0
  shimmer_animation: ^2.1.0+1

dev_dependencies:
  bloc_test: ^8.1.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.4
  network_image_mock: ^2.0.1
  remove_from_coverage: ^2.0.0
  very_good_analysis: ^2.3.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
